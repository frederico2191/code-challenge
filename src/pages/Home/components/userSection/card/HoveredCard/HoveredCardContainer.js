import React from 'react'
import HoveredCardView from './HoveredCardView'

const HoveredCardContainer = ({user}) => {
  return (
    // <CardView name={name} id={id} follower/>
    <HoveredCardView user={user}/>
  )
}

export default HoveredCardContainer